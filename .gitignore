# Compiled binaries produced by g++/clang/MSVC when following the lessons
*.exe
*.o
*.obj
*.out
*.pdb
*.ilk

# VS Code Code Runner scratch file, regenerated on every "Run Code"
tempCodeRunnerFile.*

# CMake build output for the 28-Qt-QML/ demos (regenerated by
# `cmake -B build -S .` in each demo's own folder)
build/

# Library Management System's persisted catalog (29-WrapUp-MiniProjects/),
# regenerated by running the program — not lesson content
catalog.txt
